{
  "name": "JimmyPaolini",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "root": "applications/JimmyPaolini",
  "sourceRoot": "applications/JimmyPaolini/src",
  "metadata": {
    "description": "GitHub profile README page (jimmypaolini/jimmypaolini). Contains portfolio icons, tech stack badges, and contribution stats. Not a buildable application — all targets are no-ops. Excluded from knip analysis via ignoreWorkspaces."
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No build step defined'"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No lint step defined'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No test step defined'"
      }
    },
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No run step defined'"
      }
    }
  },
  "tags": []
}
